[
  {"id":"sn-q001","domain":"Services and Networking","question":"Which statement is a requirement of the Kubernetes networking model?","choices":["Pods reach each other using NAT-translated addresses","All Pods can communicate with all other Pods on all nodes without NAT, using real Pod IPs","Only Pods on the same node can communicate directly","Pods must use the node's IP address to communicate"],"answer":1,"explanation":"The model requires all Pods to reach all Pods on every node using their real IPs with no NAT. NAT is explicitly disallowed; cross-node communication is required; Pods use their own real IPs, not the node's.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — Kubernetes Network Model"},
  {"id":"sn-q002","domain":"Services and Networking","question":"Two containers run in the same Pod. How do they communicate with each other?","choices":["Over localhost on different ports","Via the Pod's ClusterIP service","Each container gets its own Pod IP and they route between them","Through the node's bridge interface using separate IPs"],"answer":0,"explanation":"Containers in a Pod share one network namespace and one IP, so they reach each other over localhost on different ports. They do not have separate IPs and do not need a Service to talk to each other.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — Pod Networking Internals"},
  {"id":"sn-q003","domain":"Services and Networking","question":"What is the purpose of the pause (infrastructure) container in a Pod?","choices":["It runs the main application workload","It load balances traffic to the Pod","It enforces NetworkPolicy rules for the Pod","It sets up and holds the Pod's network namespace so app containers can restart without losing the network stack"],"answer":3,"explanation":"The pause container starts first, creates the network namespace, and owns it for the Pod's lifecycle, so application containers can restart without re-IPing the Pod. It does not run the app, load balance, or enforce policy.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — Pod Networking Internals"},
  {"id":"sn-q004","domain":"Services and Networking","question":"Which component is responsible for implementing the Kubernetes networking model (interfaces, IP addressing, bridges/tunnels)?","choices":["kube-proxy","The CNI plugin","CoreDNS","The kubelet's cgroup driver"],"answer":1,"explanation":"Kubernetes defines the model but abstracts implementation to the CNI plugin (Calico, Cilium, etc.). kube-proxy implements Services, CoreDNS provides DNS, and the cgroup driver is unrelated to networking.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — CNI"},
  {"id":"sn-q005","domain":"Services and Networking","question":"Which component implements Services on each node and runs as a DaemonSet?","choices":["kube-proxy","kubelet","CoreDNS","etcd"],"answer":0,"explanation":"kube-proxy implements Services (default iptables mode) and runs as a Pod via a DaemonSet, one per node. CoreDNS is DNS, kubelet manages Pods, etcd is the datastore.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — Service Networking Internals"},
  {"id":"sn-q006","domain":"Services and Networking","question":"In a Service spec, which field specifies the port the application listens on inside the container?","choices":["port","targetPort","nodePort","clusterIP"],"answer":1,"explanation":"targetPort is the container/app port. port is the Service's own listening port for clients, nodePort is the port exposed on each node, and clusterIP is the virtual IP.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — Defining Deployments and Services"},
  {"id":"sn-q007","domain":"Services and Networking","question":"What is the default Service type if none is specified?","choices":["NodePort","LoadBalancer","ClusterIP","ExternalName"],"answer":2,"explanation":"ClusterIP is the default and is internal-only. The other types must be explicitly requested.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — ClusterIP Internals"},
  {"id":"sn-q008","domain":"Services and Networking","question":"What is the default port range for a NodePort service?","choices":["1024–4999","8000–9000","30000–32767","40000–49151"],"answer":2,"explanation":"NodePort uses 30000–32767 by default. A port can be auto-assigned from this range or set statically within it.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — NodePort Internals"},
  {"id":"sn-q009","domain":"Services and Networking","question":"When you create a Service of type LoadBalancer, which other service resources are also created?","choices":["Only a ClusterIP","Only a NodePort","A NodePort and a ClusterIP","An ExternalName and a ClusterIP"],"answer":2,"explanation":"LoadBalancer builds on the lower types: it also gets a NodePort and a ClusterIP. The cloud LB sends traffic to the NodePort, which forwards to the ClusterIP, which reaches Pods.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — LoadBalancer Internals"},
  {"id":"sn-q010","domain":"Services and Networking","question":"A Service must reference a service running outside the cluster by DNS name without proxying traffic. Which type fits?","choices":["Headless service","ExternalName","NodePort","LoadBalancer"],"answer":1,"explanation":"ExternalName creates a CNAME to an external DNS name with no selector and no proxying. A headless service returns Pod IPs; NodePort/LoadBalancer expose in-cluster Pods.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — Other Types of Services"},
  {"id":"sn-q011","domain":"Services and Networking","question":"How do you define a headless service, and what does DNS return for it?","choices":["Set type: ExternalName; DNS returns a CNAME","Set type: NodePort; DNS returns the node IPs","Omit the selector; DNS returns the ClusterIP","Set clusterIP: None; DNS returns the matching Pod IPs"],"answer":3,"explanation":"A headless service has clusterIP: None. With selectors, DNS returns all matching Pod IPs so the client chooses. This is common with StatefulSets and databases.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — Other Types of Services"},
  {"id":"sn-q012","domain":"Services and Networking","question":"A ClusterIP Service shows zero endpoints. What is the most likely cause?","choices":["The Service selector does not match any running, ready Pod's labels","The cluster has no LoadBalancer integration","CoreDNS is not running","The nodePort is outside 30000–32767"],"answer":0,"explanation":"Endpoints are created from Pods whose labels match the Service selector. No matching/ready Pods means no endpoints. LoadBalancer integration, CoreDNS, and nodePort range are unrelated to empty ClusterIP endpoints.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — Understanding Services"},
  {"id":"sn-q013","domain":"Services and Networking","question":"What is the fully qualified DNS name of a Service named web in namespace prod?","choices":["web.prod.pod.cluster.local","prod.web.svc.cluster.local","web.prod.svc.cluster.local","web.svc.prod.cluster.local"],"answer":2,"explanation":"The Service FQDN pattern is service.namespace.svc.cluster.local, so web.prod.svc.cluster.local. The 'pod' segment is used for Pod records; the segment order in the other options is wrong.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — Cluster DNS Records"},
  {"id":"sn-q014","domain":"Services and Networking","question":"Where is CoreDNS configured in a default Kubernetes cluster?","choices":["A ConfigMap named coredns in the kube-system namespace","A Secret named coredns in the default namespace","The /etc/kubernetes/coredns.conf file on each node","A CRD named CoreDNSConfig"],"answer":0,"explanation":"CoreDNS reads a ConfigMap named coredns in kube-system, mounted as the Corefile at /etc/coredns. It is not a Secret, a node file, or a CRD.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — Cluster DNS"},
  {"id":"sn-q015","domain":"Services and Networking","question":"What is the default dnsPolicy for a Pod, and what does setting dnsPolicy: None require?","choices":["Default is None; None requires nothing extra","Default is ClusterFirst; None requires a dnsConfig block defining nameservers/searches","Default is Default; None disables DNS entirely","Default is ClusterFirstWithHostNet; None inherits the node's resolver"],"answer":1,"explanation":"ClusterFirst is the default (cluster domain to cluster DNS, rest to node DNS). dnsPolicy: None ignores cluster DNS and requires a dnsConfig. The literal value 'Default' means inherit from the node and is not the actual default.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — Custom Pod DNS Configuration"},
  {"id":"sn-q016","domain":"Services and Networking","question":"Why is DNS preferred over environment variables for service discovery?","choices":["Env vars only work for LoadBalancer services","Env vars are injected only for Services that existed at Pod startup, so later Services require recreating the Pod; DNS updates dynamically","DNS is faster than env vars at the kernel level","Env vars cannot store IP addresses"],"answer":1,"explanation":"Service env vars are a snapshot at Pod startup; a Service created afterward won't appear until the Pod is recreated. DNS resolves dynamically without restarting Pods. The other claims are false.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — Service Discovery"},
  {"id":"sn-q017","domain":"Services and Networking","question":"Which statement correctly distinguishes an Ingress resource from an Ingress controller?","choices":["The Ingress resource defines the rules; the Ingress controller is the reverse proxy that implements them and must be deployed separately","The Ingress resource routes traffic; the controller only stores rules","Both are deployed automatically with every cluster","The Ingress controller defines hosts/paths; the resource forwards packets"],"answer":0,"explanation":"The Ingress resource holds the rules (hosts, paths, TLS); the controller is the actual reverse proxy that reads them and routes traffic, and it must be installed (clusters don't ship one). The other options invert or misstate these roles.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — Ingress Architecture"},
  {"id":"sn-q018","domain":"Services and Networking","question":"An Ingress rule uses pathType: Exact with path /blue. Which request matches?","choices":["/Blue","/blue/1","/blue","/blueprint"],"answer":2,"explanation":"Exact requires an exact, case-sensitive match. Only /blue matches. /Blue differs by case, and /blue/1 and /blueprint are different paths (those would only match under Prefix for /blue).","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — Path-Based Routing"},
  {"id":"sn-q019","domain":"Services and Networking","question":"Which is a valid reason to use Ingress instead of a Layer-4 LoadBalancer service?","choices":["Ingress can load balance UDP traffic by IP and port","Ingress works at Layer 7, enabling host- and path-based routing while fronting many services on one IP","Ingress always provisions a dedicated public IP per service","Ingress cannot terminate TLS"],"answer":1,"explanation":"Ingress is Layer 7 (HTTP), supporting name-based vhosts and path routing and exposing many services behind one IP. LoadBalancer services are Layer 4 (one service per IP/port). Ingress can terminate TLS, and it is used for HTTP, not arbitrary UDP.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — Why Use Ingress Rather Than Load Balancers"},
  {"id":"sn-q020","domain":"Services and Networking","question":"In the Gateway API, which resource defines protocol-specific routing rules and attaches to a Gateway?","choices":["GatewayClass","HTTPRoute","IngressClass","Service"],"answer":1,"explanation":"HTTPRoute (and TCPRoute, GRPCRoute, etc.) defines routing rules and attaches to a Gateway via parentRefs. GatewayClass defines the controller-backed class, IngressClass belongs to the older Ingress API, and a Service is a backend target.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — Ingress Architecture (Kubernetes docs)"},
  {"id":"sn-q021","domain":"Services and Networking","question":"How does a NetworkPolicy behave once it selects a Pod for the Ingress direction?","choices":["It allows all ingress traffic to that Pod","Ingress becomes default-deny for that Pod, and only explicitly allowed traffic is permitted","It blocks all egress as well, automatically","It has no effect unless every namespace has a policy"],"answer":1,"explanation":"Selecting a Pod for a direction flips that direction to default-deny; only traffic matched by the policy's rules is allowed. It does not affect egress unless Egress is in policyTypes, and policies are additive across namespaces.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — CNI (Kubernetes docs)"},
  {"id":"sn-q022","domain":"Services and Networking","question":"A NetworkPolicy object exists but appears to have no effect. What is a likely reason?","choices":["NetworkPolicies only apply to LoadBalancer services","The policy must be created in kube-system to take effect","The cluster's CNI plugin does not support NetworkPolicy enforcement","NetworkPolicies require a Gateway to be deployed"],"answer":2,"explanation":"NetworkPolicy enforcement depends on a supporting CNI (Calico, Cilium, etc.); with a non-enforcing CNI, policies are created but ignored. Policies apply to Pods (not just LoadBalancer services), work in any namespace, and have nothing to do with Gateways.","source":"Configuring and Managing Kubernetes Networking, Services, and Ingress — CNI (Kubernetes docs)"}
]
